## Zaeli App — New Chat Handover Brief
*23 March 2026 — copy this entire message to start a new chat*

---

Hi! I'm continuing development of the **Zaeli app** — a React Native / Expo iOS-first family life platform with AI at its core. We've been building this together across many sessions — please pick up exactly where we left off.

---

### Who you are talking to
- **Richard** — beginner developer. Full file rewrites always, easy copy-paste PowerShell commands, step by step
- Family: Anna (logged-in user), Richard, Poppy (Yr6, 12), Gab (Yr4, 10), Duke (Yr1, 8)
- Path: `C:\Users\richa\zaeli` (Windows, PowerShell — no `&&` chaining)
- Repo: https://github.com/RDK1981/zaeli (private)

---

### Key docs — read these first
- `CLAUDE.md` — full stack, rules, screen status, colour system
- `ZAELI-PRODUCT.md` — product framework, architecture, business model
- `ZAELI-PERSONA.md` — Zaeli's voice, tone, persona rules

---

### Key constants
- `DUMMY_FAMILY_ID = '00000000-0000-0000-0000-000000000001'`
- `DUMMY_MEMBER_NAME = 'Anna'`
- GPT: `gpt-5.4-mini` with `max_completion_tokens`
- Claude: `claude-sonnet-4-6` with `max_tokens` (vision only)
- Supabase: `rsvbzakyyrftezthlhtd` (Sydney)
- Admin: https://incomparable-gumdrop-32e4ba.netlify.app

---

### Domains (purchased 23 March 2026 via Cloudflare)
- **zaeli.app** — app destination
- **zaeli.ai** — marketing website

### Apple Developer Account
- Individual account registered 23 March 2026 — $144.99 AUD/year
- **Status: Pending Apple approval** (check email — may take 24-48hrs)
- Once approved: App Store Connect → TestFlight → dev build

---

### What was built this session (23 March 2026)

#### index.tsx — COMPLETE REDESIGN
The home screen is now a full AI-first chat interface. This was the biggest change of the entire project.

**Architecture:**
- Light/dark mode via `useColorScheme()` — fully themed via `L` and `D` token objects
- Light: coral banner `#E8503A`, white body, neutral grey bubble `#F2F2F2`
- Dark: navy banner `#0D1B3E`, true black `#111111`, dark bubble `#1E2D50`
- Zaeli ✦ star always `#4D8BFF` (light blue — AI identity, never changes)
- Coral = send button only — nothing else

**Locked design decisions:**
- No accent colours in Zaeli text — plain ink only
- No pills — replaced by 3×3 grid icon (⊞) above input bar
- Quick replies (3 chips) replace CTAs — generated by GPT alongside brief
- "All good →" as muted dismiss text
- No bottom tab bar — hamburger navigation only
- Walkie-talkie voice — auto-sends after Whisper transcription
- Eyebrow (✦ Zaeli · timestamp) — first message in sequence only
- Sentences split on full stops with 10px gap between
- Grid tap injects seed message into chat (not navigation)
- Live camera replaces "Files" in + sheet
- Font: 17px / 27px line height

**Brief JSON format:**
```json
{"main":"Evening, Anna — ...","replies":["chip 1","chip 2","chip 3"],"seed":"..."}
```

**buildContext() fetches in one Promise.all:**
- Shopping item names + count (up to 50)
- Calendar events (next 5)
- Todo count + Meal plan

#### ZAELI-PERSONA.md — NEW FILE
Full persona spec written and saved. Key additions vs old persona:
- Camaraderie added — "we've got this", genuine teammate energy
- Emojis unlocked — 1-2 per message, purposeful
- "Mate" explicitly banned
- No hollow affirmations
- Vary tone deliberately — never same rhythm twice
- Moments of delight — "sorted — future you will be very pleased about that 🙌"
- Viral moment test: would Anna screenshot this and send it to a friend?

#### zaeli-chat.tsx — channel personas updated
General, Calendar, Shopping, Meals all rewritten with new Zaeli voice.

---

### Design decisions made via HTML mockups this session

All mockups saved in outputs folder:
- `zaeli-layout-concepts.html` — text/spacing/bubble/banner/pills/live camera
- `banner-compare.html` — 4 banner shades
- `zaeli-clean-concepts.html` — final clean system light & dark
- `colour-options.html` — 4 colour options (Anna approved Option B)

**Key design journey:**
1. Started with electric blue banner — felt too masculine/corporate
2. Explored transparent banner, cream body, white body
3. Explored 9 pills → 5 pills → grid icon (cleaner)
4. Explored CTAs → quick replies (smarter, more AI-native)
5. Anna said "looks like a boys app" — went through colour options
6. Landed on coral banner (Option B) — warm, distinctive, gender-inclusive
7. Coral keyboard issue in Expo Go — not fixable without dev build (cosmetic only)

---

### Outstanding issues / known bugs

| Issue | Status | Notes |
|-------|--------|-------|
| Coral keyboard tint | ⚠️ Expo Go only | Fixed in dev build — not a real bug |
| zaeli-chat.tsx design | ⚠️ Old style | Still has old blue bubbles, needs update |
| expo-av deprecated | ⚠️ Warning only | Migrate to expo-audio/video at SDK 54 |
| expo-notifications | ⚠️ Expo Go only | Use dev build |

---

### Immediate next steps (priority order)

1. **Get Apple Developer approval** — check email, complete any verification
2. **Build development build** — `npx expo run:ios` — fixes coral keyboard, unlocks real device testing
3. **Update zaeli-chat.tsx** — bring in line with new home screen design (coral banner, no old blue bubbles, new persona)
4. **Meal Planner screen** — connect to Supabase, replace dummy data
5. **API usage logging** — Supabase table + wrapper for per-family cost tracking
6. **Live camera proper build** — simultaneous `expo-camera` preview + `expo-av` recording. Currently opens photo picker only — the real feature needs custom camera view
7. **Marketing website** — zaeli.ai landing page
8. **TestFlight beta** — once dev build and Apple approval sorted

---

### Feature ideas discussed but not built

- **Smart contextual quick reply chips** below non-brief Zaeli messages (adapts to conversation)
- **Live camera + simultaneous voice** — true "point and ask" — camera preview open while mic records, both sent to Zaeli together
- **Kids Hub** — child-specific context, homework helper (Socratic method, grade-aware)
- **Dark mode toggle** — currently follows system, should be user-selectable

---

### Things that DO NOT need revisiting
- No floating FAB anywhere
- No bottom tab bar
- Logo on every screen taps → home
- Brief: thinking dots → fade in (no typewriter)
- Coral = send button and nothing else
- Grid icon taps inject chat message (not navigate)
- No accent colours in Zaeli message text

---

### Tech reminders
- `expo-file-system`: always `expo-file-system/legacy`
- `SafeAreaView` always `edges={['top']}`
- Poppins for all UI, DMSerifDisplay for logo only
- Always `npx expo start --clear` after copying files
- PowerShell: no `&&` — separate lines only
- `gpt-5.4-mini` is correct model string
- OpenAI: `max_completion_tokens` | Claude: `max_tokens` — NEVER mix

---

## Session update — 24 March 2026

### Entry flow built (A1 splash → E2 entry → recording → chat)

**Splash (State 1 — 1.5s):**
- Full `#4D8BFF` blue screen
- Coral star box (`#FF6B55`) spring-bounces in — `scale(0.4)` → `scale(1)`
- Wordmark fades in 350ms after star lands
- Typing dots pulse while brief pre-loads
- After 1.5s → fades to entry screen

**Entry (State 2 — resting):**
- Big centred two-line greeting — time-based:
  - Morning: "Morning, Anna! / How can I help you today?"
  - Afternoon: "Hey, Anna! / How can I help you this afternoon?"
  - Evening: "Evening, Anna! / How can I help you tonight?"
  - Late: "Hey, Anna! / What do you need before tomorrow?"
- Prominent coral mic card — static waveform, `Speak to Zaeli` title
- "or tap a topic" divider
- 5 focus chips (Today · Shopping & meals · What needs doing · Kids & family · What's coming up)
- **Identical chat bar at bottom** — same barPill, same + icon, same mic, same send. Placeholder: "Ask Zaeli anything…"

**Recording (State 3 — full screen):**
- Tapping mic card → `handleEntryMicStart()` → `entryRecording = true`
- Chips and content fade away, full-screen recording UI takes over
- "Listening…" in large italic serif
- 88px coral mic circle with two animated pulse rings
- 13-bar waveform across full width
- "Tap the mic when you're done" hint
- Tapping mic → `handleEntryMicStop()` → Whisper transcribes

**Chat (State 4):**
- Entry fades out, chat fades in
- Voice transcript added as first user message with 🎤 "Voice message" label above bubble
- `generateBrief(true, transcript)` fires — Zaeli responds focused on what was said
- If chip tapped → `generateBrief(true, chipSeed)` — brief focused on topic
- If typed → text enters chat as normal message

### Design fixes made
- Blue banner restored `#4D8BFF`
- Zaeli ✦ star + name: `#FF7B6B` light coral
- 9-dot grid: `#FF4545` full coral with coral-tinted bg
- Bar placeholder + plus icon: `rgba(10,10,10,0.5)` — darker/readable
- White body (not cream)
- Font: 17px / 27px line height on all messages
- Coral keyboard (Expo Go only — fixed in dev build)
- `setKbHeight` error removed
- `ctx` reference error fixed

### Colour decision pending
Anna wants to move away from blue. Four complete palettes explored (Peach, Sage, Lavender, Sunshine) plus a mixed-palette system (Direction 3 — different combo per session). Decision deferred — will revisit after core features are stable.

### Immediate next steps
1. **Test entry flow on device** — splash → entry → recording → chat
2. **Dev build** — `npx expo run:ios` once Apple Developer approved — fixes coral keyboard
3. **Rich message rendering** — calendar grids, shopping lists, todos inline in chat
4. **zaeli-chat.tsx design update** — bring in line with new home screen
5. **Meal Planner** — connect to Supabase
6. **API usage logging**
7. **Colour palette decision** — mixed palette system ready to code once decided

### Apple Developer Account
- Registered 23 March 2026, Individual, $144.99 AUD/year
- **Awaiting Apple approval** — check email
